  <div class="flex-grid-links">
    <div class="col navigation">
        <%= link_to 'Summary', "/trips/#{@trip.id}/" %>
    </div>
    <div class="col navigation">
        <%= link_to 'Add Expense', add_expense_path(@trip.id) %>
    </div>
  </div>

<div class="flex-grid">
  <div class="col">
    <h1><%= @trip.name %></h1>
  </div>
</div>

<div class="flex-grid">
  <div class="col">
    <%= doughnut_chart @data , { responsive: true }  %>
  </div>
</div>