<%= render 'shared/error_messages', locals: { errors: @trip.errors } %>

<%= form_with(model: expense_category, local: true) do |form| %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, autofocus: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
